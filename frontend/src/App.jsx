import AppRoutes from '@/routes'

/**
 * App — renders the full route tree.
 * All providers (BrowserRouter, QueryClient, AuthProvider) are in main.jsx.
 */
export default function App() {
  return <AppRoutes />
}
