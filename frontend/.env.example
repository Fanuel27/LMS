# Vite API Proxy Target (for local development)
# The frontend dev server proxies /api/* to this URL.
# In production this is NOT needed — the frontend talks directly to the backend.
# VITE_API_URL=http://localhost:3001

VITE_API_URL=https://national-exam-backend.onrender.com/api